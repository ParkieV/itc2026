# Инфра (прокся итд итп) будет тут

## Шаги для подготовки 

1. Создайте файл `db.env` и заполните его по формату из `db.env.example`

```
POSTGRES_DB=db
POSTGRES_USER=user
POSTGRES_PASSWORD=strongpassword
```